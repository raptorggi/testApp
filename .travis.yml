services:
  - postgresql
before_script:
  - psql -c 'create database testApp_test;' -U postgres
language: ruby
rvm:
  - 2.4.2
script:
  - bundle install
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec